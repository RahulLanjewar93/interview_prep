const parenthesis = (str) => {
    str = str.split("")

    str.forEach((char,index)=>{
        if(char == "{" || char  == "[" || char == "("){

        }
    })
}

console.log(parenthesis('[([({})])]'))